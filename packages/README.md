# Shared Packages

Internal libraries and shared config live here (e.g. UI components, TypeScript configs, ESLint presets).

Create each shared library as its own subdirectory with a `package.json` so pnpm and Turborepo can discover it.

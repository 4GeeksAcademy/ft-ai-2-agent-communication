{
  "name": "@area-todo/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn area_todo_api.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "uv run python -m compileall src",
    "lint": "uv run ruff check src tests",
    "test": "uv run pytest",
    "format": "uv run autopep8 --in-place --recursive src tests && uv run ruff format src tests"
  }
}
